<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/aboutme_item_height"
    android:layout_marginStart="@dimen/activity_horizontal_margin" android:layout_marginVertical="@dimen/aboutme_item_dis"
    android:layout_marginLeft="@dimen/activity_horizontal_margin">

    <!-- TODO: Update blank fragment layout -->
    <ImageView
        android:id="@+id/item_icon"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:contentDescription="TODO" android:src="@drawable/aboutme"/>
    <TextView
        android:id="@+id/aboutme_item"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="5"
        android:text="@string/hello_blank_fragment" />

</LinearLayout>